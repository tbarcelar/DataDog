
$ preparar ubuntu para receber datadog
apt-get update
apt-get install apt-transport-https  
apt install curl
apt install gnupg

$ baixar agent
docker run -d --name dd-agent -v /var/run/docker.sock:/var/run/docker.sock:ro -v /proc/:/host/proc/:ro -v /sys/fs/cgroup/:/host/sys/fs/cgroup:ro -e DD_API_KEY=xxxxxxxxxxxx -e DD_SITE="datadoghq.com" gcr.io/datadoghq/agent:7


$ verificar status do agent
service datadog-agent status

$ configurar host
cd /etc/datadog-agent
vi datadog.yaml
$ se não houver o vim instalar  > apt install vim


